interface:
  display_name: "Template SDS Generator"
  short_description: "Generate traceable SDS packages from a template"
  default_prompt: "Use $template-sds-generator to generate a template-preserving SDS package from a DOCX template, a rule file, and 1-3 source SDS documents."
policy:
  allow_implicit_invocation: true
