# runtime-config

Editable local config copies live here.

Recommended files:

- `openclaw.plugin.json`: sanitized OpenClaw plugin config to import or merge into your local OpenClaw setup.
- `windows-sidecar.toml`: local sidecar config passed to `oc-wx-sidecar --config`.

Do not commit live secrets or machine-specific paths into this directory.
