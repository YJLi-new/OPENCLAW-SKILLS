# Architecture (docs copy)

Primary architecture document:

- `../ARCHITECTURE.md`

Summary:

- Plugin-first control plane
- Bundled skill for operator-facing usage
- Windows sidecar adapter boundary for WeChat desktop automation
- BHMailer API-first adapter with optional webhook push mode
- SQLite state for dedupe, jobs, commands, watch subscriptions

